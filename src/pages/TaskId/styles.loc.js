import { css } from "reshadow/macro"

export const label = css`
  label {
    display: grid;
    grid-gap: 8px;
    font-size: 14px;
    font-weight: 500;
    color: rgba(var(--main), 0.6);
  }
`
export const row = css`
  row {
    display: grid; 
    grid-gap: 16px;
  }
`
