import { css } from "reshadow"

export const svg = css`
  svg {
    width: 16px;
    height: 16px;
    color: currentColor;
  }
  path: {
    fill-rule: ovenodd;
    clip-rule: ovenodd;
  }
`
export const span_wrap = css`
  span {
    display: inline-flex;
  }
`
