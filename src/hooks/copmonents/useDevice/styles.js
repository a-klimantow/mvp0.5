import { css } from "reshadow/macro"

export default css`
  device {
    display: inline-flex;
  }
  span {
    color: var(--caption-color);
    margin-left: 4px;
  }
  svg {
    margin-right: 8px;
  }
`
